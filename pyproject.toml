[project]
name = "pay-tracker-filter"
version = "0.1.0"
description = "Simple invoice filtering for pay-tracker"
requires-python = ">=3.8"

[tool.pytest.ini_options]
minversion = "6.0"
addopts = "-q"
testpaths = ["tests"]
